export const metadata = {
  title: 'Events',
  description: 'Upcoming Thai dance performances, workshops, and cultural events from Dara Thai Dance School in Perth.',
};

export default function Layout({ children }) {
  return children;
}
