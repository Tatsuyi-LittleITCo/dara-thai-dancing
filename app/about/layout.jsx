export const metadata = {
  title: 'About Us',
  description: 'Learn about Dara Thai Dance School in Perth — our history, mission, and the cultural arts we share.',
};

export default function Layout({ children }) {
  return children;
}
